package com.fuad.view.deck;

public record DeckAction(
        String id,
        String symbol,
        String name,
        String detail,
        String voiceHint) {
    /* Empty intentionally */
}
